<%- include("partials/header"); -%>

    <h1> Home</h1>
    <p>
        <%= startingContent %>
    </p>
    <% composeText.forEach(function(element){ %>
         <% console.log(element.title); %>
         <h1> <%= element.title %> </h1>
         <p> <%= element.content.substring(0,100) + "..." %><a href="/posts/<%= element.title %>">
            Read more</a> </p>
    <% } ) %>

<%- include("partials/footer"); -%>